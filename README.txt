This is a fork of fipolate, which you can find here:
http://xyne.archlinux.ca/projects/fipolate/

The goal of this project is to add a bit more flexibility to the
script, allowing users to (for example) close the pipe after one use
of the pipe.
